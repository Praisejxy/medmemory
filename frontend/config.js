// Set this to your deployed Alibaba Cloud Function Compute HTTP trigger
// endpoint after deployment (see docs/DEPLOY.md). Keeping it in its own
// file means updating the backend URL never requires touching index.html.
window.MEDMEMORY_BACKEND_URL = "http://localhost:8000/";
